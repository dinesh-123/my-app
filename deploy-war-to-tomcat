node{
   stage('SCM Checkout'){
     git 'https://github.com/dinesh-123/my-app.git'
   }
   stage('Compile-Package'){
      // Get maven home path
      sh "/usr/share/maven/bin/mvn package"
   }
   stage('Deploy to Tomcat'){
   
      sshagent(['tomcat-dev']) {
         sh 'scp -o StrictHostKeyChecking=no target/*.war ubuntu@18.191.253.235:/var/lib/tomcat8/webapps/'
      }
    }
  }
  
